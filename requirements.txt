# Core dependencies 
python-dotenv
supabase

# FastAPI & server
fastapi
uvicorn[standard]
pydantic>=2.0

# Scheduler (replaces Vercel cron)
apscheduler>=3.10

# OpenAI-compatible providers (Phase 0 & Phase 2)
openai
groq
together
openrouter

# Custom SDK providers (Phase 3)
anthropic
google-genai

# Utilities
httpx
requests
beautifulsoup4
tiktoken  # Token count estimation and validation
PyJWT>=2.0  # Supabase JWT verification

# Local database (PostgreSQL)
psycopg2-binary
